class Calculate:
    """Title

    Description

    Attributes:
        @ (type): @
    """
    @staticmethod
    def add(a: int, b: int) -> int:
        """Title

        This function calculates the sum of two objects and returns it.

        Args:
            a (int): is the first object
            b (int): is the second object

        Returns:
            int: 

        Raises:
            (name): @

        Yields:
            (type): @

        Examples:
            >>> print(add(5, 2))
            7

        Note:
            @
        """
        return a + b

if __name__ == '__main__':
    help(Calculate.add)
